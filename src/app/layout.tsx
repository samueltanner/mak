"use client"
import "./globals.css"
import { Inter } from "next/font/google"
import { MakUiComponentConfig } from "./_hooks/useMakUi/types/default-types"
import { MakUiProvider } from "./_hooks/useMakUi/context/MakUiContext"
import {
  MakUiFlexiblePaletteInput,
  MakUiPaletteInput,
} from "./_hooks/useMakUi/types/ui-types"

const inter = Inter({ subsets: ["latin"] })

// export const metadata: Metadata = {
//   title: "Create Next App",
//   description: "Generated by create next app",
// }
const palette: MakUiFlexiblePaletteInput = {
  primary: {
    base: "purple-500",
    hover:
      "purple-600 dark:purple-400 dark:hover:enabled:red-500 disabled:gray-400",
  },
  // secondary: {
  //   base: "hover:green",
  // },
  text: "dark:zinc-50 zinc-950 hover:enabled:open:blue-900",
  // theme: "purple dark:zinc-900",
}

const componentConfig: MakUiComponentConfig = {
  buttonConfig: {
    className:
      "px-2 py-1.5 h-fit w-fit text-sm rounded-md font-semibold border border-[3px]",
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <MakUiProvider palette={palette} componentConfig={componentConfig}>
          {children}
        </MakUiProvider>
      </body>
    </html>
  )
}
